package com.scottwehby.fetchexercise.data.model

/**
 * This is the exact data retrieved from the server, unaltered.
 */
data class ItemDto(
    val id: Int?,
    val listId: Int?,
    val name: String?
)
